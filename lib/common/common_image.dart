import 'package:flutter/material.dart';

class CommanImage extends StatelessWidget {
  final String imageURL;
  final Size size;
  const CommanImage({super.key, required this.imageURL, required this.size});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
